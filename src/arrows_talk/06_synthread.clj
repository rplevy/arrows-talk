(ns arrows-talk.06-synthread
  (:require [lonocloud.synthread :as ->]))


(comment
  * ->/assoc
  * ->/in
  * ->/for
  * ->/when
  * ->/aside
  * ->/reset
  * ->/apply )


(comment

  LonoCloud/synthread is a great threading macro library.
  I didn't have time to fit it into a lightning talk, but
  check it out.

  https://github.com/LonoCloud/synthread )
